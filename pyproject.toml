[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ensam3d-lib"
version = "0.2.0"
readme = "README.md"
license = {file = "LICENSE"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["ensam3d_inference*"]